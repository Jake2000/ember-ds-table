{{#if hasBlock}}
    {{yield}}
{{else}}
    {{#if column.component}}
        {{component column.component
            value=value
            column=column
        }}
    {{else}}
        {{value}}
    {{/if}}
{{/if}}
